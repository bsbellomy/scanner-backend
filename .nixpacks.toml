[phases.setup]
nixPkgs = ["git", "cmake", "make", "pkg-config", "gcc", "g++", "python3"]

[phases.install]
cmds = [
    "git --version || true",   # make sure git is available
    "npm install"
]

[phases.build]
cmds = ["npx opencv-build-npm rebuild --jobs max"]

[start]
cmd = "npm start"
